[project]
name = "afw-agent-engine"
version = "0.1.0"
description = "Python agent engine for Agent Fantasy World"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
