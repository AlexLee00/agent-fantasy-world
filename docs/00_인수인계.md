# 🏰 AFW 인수인계 문서 v6
**날짜:** 2026-04-05 (세션 6 종료)
**GitHub:** https://github.com/AlexLee00/agent-fantasy-world

---

## ⚡ 새 채팅 시작 시 바로 이것부터

```
"AFW 컨트랙트 컴파일 에러 수정하고 테스트 작성해줘"
```

---

## ✅ 이번 세션(6) 완료 항목 — GitHub 설정 100% 완료

### GitHub Actions Workflows (4개)
```
.github/workflows/ci.yml           ← 기존
.github/workflows/security.yml     ← ✅ NEW (CodeQL + Dependency Review)
.github/workflows/stale.yml        ← ✅ NEW (30일 비활동 자동 정리)
.github/workflows/label-sync.yml   ← ✅ NEW (labels.json 변경 시 자동 동기화)
```

### Labels (26개 — 기본 라벨 6개 삭제 후 재구성)
```
type/       bug, feature, enhancement, docs, chore, security, aip (7)
priority/   critical, high, medium, low (4)
status/     stale, blocked, in-progress, ready, needs-review (5)
scope/      contracts, frontend, agents, tokenomics, infra, world (6)
기타        good first issue, help wanted, pinned, wontfix (4)
```

### Branch Protection — 적용 완료
```
main:
  ✅ PR 1명 승인 필수
  ✅ CODEOWNERS 리뷰 필수
  ✅ CI status checks 필수 (strict)
  ✅ Stale reviews 자동 dismiss
  ✅ Linear history 강제
  ✅ Force push / deletion 차단
  ✅ Conversation resolution 필수

develop:
  ✅ PR 1명 승인 필수
  ✅ CI status checks 필수
  ✅ Force push 차단
  ✅ Conversation resolution 필수
```

### PR 머지 전략 — 적용 완료
```
✅ Squash merge ON (기본 — PR title + body)
✅ Rebase merge ON
❌ Merge commit OFF
✅ Auto-delete branches ON
✅ Auto-merge ON
✅ Discussions ON / Wiki OFF
```

### Security — 적용 완료
```
✅ Vulnerability alerts
✅ Dependabot automated security fixes
✅ Secret scanning + push protection
✅ CodeQL 정적 분석 (워크플로우)
✅ Dependency review (PR마다)
✅ dependabot.yml (기존)
```

---

## 🔥 다음 세션에서 바로 할 것 — 컨트랙트 컴파일 & 테스트

### 1단계: WorldMap.sol 유니코드 수정 (컴파일 에러)
```
파일: packages/contracts/src/world/WorldMap.sol
문제: 한국어 문자열에 unicode 리터럴 누락
수정: "빛의 베일" → unicode"빛의 베일" (4곳)
     Line 44: "빛의 베일"   → unicode"빛의 베일"
     Line 51: "회색 행진"   → unicode"회색 행진"
     Line 54: "잿불 지하"   → unicode"잿불 지하"
     Line 57: "공허의 끝"   → unicode"공허의 끝"
```

### 2단계: 재컴파일
```bash
cd /Users/alexlee/projects/agent-fantasy-world/packages/contracts
NODE_ENV=development npx hardhat compile
```
⚠️ NODE_ENV=production이 기본 설정이라 devDependencies 설치 시
반드시 `NODE_ENV=development npm install` 또는 `npm install --include=dev` 사용

### 3단계: 테스트 코드 작성
```
test/
  ├── AFWToken.test.ts
  ├── SOULToken.test.ts
  ├── AgentRegistry.test.ts
  ├── QuestEngine.test.ts
  ├── EconomyEngine.test.ts
  ├── OracleGateway.test.ts
  ├── NodeRegistry.test.ts
  ├── GovernanceDAO.test.ts
  └── WorldMap.test.ts
```

### 4단계: 미완성 컨트랙트 추가
```
⬜ CombatResolver.sol — 전투 판정 시스템
⬜ ItemRegistry.sol   — 아이템 등록/장착
```

---

## 📁 현재 레포 구조 (커밋 완료 상태)

```
agent-fantasy-world/
├── .github/
│   ├── CODEOWNERS
│   ├── PULL_REQUEST_TEMPLATE.md
│   ├── dependabot.yml
│   ├── labels.json              ← ✅ NEW
│   ├── ISSUE_TEMPLATE/
│   │   ├── aip_proposal.yml
│   │   ├── bug_report.yml
│   │   ├── feature_request.yml
│   │   └── world_content.yml
│   └── workflows/
│       ├── ci.yml
│       ├── security.yml         ← ✅ NEW
│       ├── stale.yml            ← ✅ NEW
│       └── label-sync.yml       ← ✅ NEW
├── docs/
├── packages/
│   ├── contracts/
│   │   ├── hardhat.config.ts
│   │   ├── tsconfig.json        ← ✅ NEW (로컬만)
│   │   ├── package.json
│   │   ├── src/
│   │   │   ├── core/
│   │   │   │   ├── AFWToken.sol      ✅
│   │   │   │   └── SOULToken.sol     ✅
│   │   │   ├── game/
│   │   │   │   ├── AgentRegistry.sol ✅
│   │   │   │   └── QuestEngine.sol   ✅
│   │   │   ├── economy/
│   │   │   │   └── EconomyEngine.sol ✅
│   │   │   ├── node/
│   │   │   │   ├── NodeRegistry.sol  ✅
│   │   │   │   └── OracleGateway.sol ✅
│   │   │   ├── governance/
│   │   │   │   └── GovernanceDAO.sol ✅
│   │   │   └── world/
│   │   │       └── WorldMap.sol      ⚠️ 유니코드 수정 필요
│   │   ├── test/                     ⬜ 비어있음
│   │   └── scripts/
│   │       └── deploy.ts
│   └── world/
├── scripts/
│   └── github-setup.sh          ← ✅ NEW
├── CODE_OF_CONDUCT.md
├── CONTRIBUTING.md
├── LICENSE
├── README.md
└── SECURITY.md
```

---

## 🛠 개발 환경 메모

```
Node.js:     v25.8.2
npm:         11.11.1
Hardhat:     2.28.6
Solidity:    0.8.20
OpenZeppelin: 5.6.1

⚠️ NODE_ENV=production이 전역 설정
   → npm install 시 반드시 NODE_ENV=development 또는 --include=dev
   → npx hardhat 실행 시도 NODE_ENV=development 권장

⚠️ Node v25 — Hardhat 공식 미지원 경고 있음 (작동은 됨)
```

---

## 📐 컨트랙트 아키텍처 (변경 불가)

```
AgentRegistry ← OracleGateway ← NodeRegistry
     ↓               ↓
QuestEngine      EconomyEngine
     ↓               ↓
CombatResolver   SOULToken (mint/burn)    ← ⬜ 미작성
     ↓
ItemRegistry                              ← ⬜ 미작성
     ↓
GovernanceDAO (파라미터 제어)
```

---

## 🚀 전체 로드맵 진행률

```
Phase 0 — Foundation (현재)
  [✅] GitHub 레포 + 커뮤니티 문서
  [✅] GitHub 설정 (라벨, 브랜치 보호, 보안, 머지전략)
  [✅] 핵심 컨트랙트 작성 (8/10)
  [⬜] 컨트랙트 컴파일 검증
  [⬜] 테스트 코드 작성
  [⬜] CombatResolver + ItemRegistry
  [⬜] Deploy 스크립트
  [⬜] 에이전트 AI 엔진 프로토타입
  [⬜] React 클라이언트 Vite 세팅

Phase 1 — Genesis
  [⬜] 테스트넷 배포 (Polygon Amoy)
  [⬜] 에이전트 1마리 라이브 데모
  [⬜] Genesis Hackathon
```

---

## 🔑 세션 시작 명령어

```bash
cd /Users/alexlee/projects/agent-fantasy-world/packages/contracts
NODE_ENV=development npx hardhat compile
```

그 다음: `"WorldMap.sol 유니코드 수정 → 컴파일 → 테스트 작성"`

---

*AFW 인수인계 v6 — 2026-04-05 (세션 6) | "A World That Lives" 🏰*
